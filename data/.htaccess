# Allow JSON files (needed for frontend)
<FilesMatch "\.json$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Block log files
<FilesMatch "\.log$">
    Order Deny,Allow
    Deny from all
</FilesMatch>
